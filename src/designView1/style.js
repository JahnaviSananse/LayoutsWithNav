import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  view1: {borderWidth: 3, borderColor: 'black'},

  redText: {
    fontSize: 30,
    borderWidth: 10,
    borderColor: 'red',
    margin: 20,
    textAlign: 'center',
    paddingTop: 20,
  },
});
